/**
 * @author Artem Lytvynov
 * @copyright Artem Lytvynov
 * @license Apache2.0
 */

namespace enum;

/// `HDML` network protocol query decimal data field bit width enum.
enum DecBitWidth: byte {
  _128,
  _256,
}