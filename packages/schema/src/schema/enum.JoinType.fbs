/**
 * @author Artem Lytvynov
 * @copyright Artem Lytvynov
 * @license Apache2.0
 */

namespace enum;

/// Query join types enum.
enum JoinType: byte {
  Cross,
  Inner,
  Full,
  Left,
  Right,
  FullOuter,
  LeftOuter,
  RightOuter,
}